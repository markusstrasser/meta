# Dead LLM-backed hooks/jobs — class diagnosis + fixes (2026-07-06)

SEARCH_ROOT: /Users/alien (hooks under ~/Projects/skills/hooks, ~/Projects/*/.claude, ~/.claude)
GLOB_SCOPE: ~/Projects
VERDICT_REQUIRES: content-match

Status: STUB — findings appended below.

## Assigned instances
1. prior-context front-load hook — see "Root cause" section.
2. clash-detect shadow (no data) — see "Root cause" section.

(appended after investigation)
