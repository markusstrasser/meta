Saved the audit to [codex-receipt-drift.md](/Users/alien/Projects/meta/docs/audit/codex-receipt-drift.md).

Most schema fields at [agent_receipts.py](/Users/alien/Projects/meta/scripts/agent_receipts.py#L287) still parse the audited session correctly, including token totals and `reasoning_effort` via [agent_receipts.py](/Users/alien/Projects/meta/scripts/agent_receipts.py#L245). The real drift is outside the existing schema: [agent_receipts.py](/Users/alien/Projects/meta/scripts/agent_receipts.py#L265) ignores `task_started` and `event_msg.user_message`, [agent_receipts.py](/Users/alien/Projects/meta/scripts/agent_receipts.py#L257) ignores `developer` messages, and there is no receipt field for `cli_version`, launch provenance, approval/sandbox context, `model_context_window`, or `reasoning_item_count`.

The dashboard does not break today because it only reads fields the parser still emits, but [dashboard.py](/Users/alien/Projects/meta/scripts/dashboard.py#L173) reparses Codex sessions live instead of consuming the persisted Codex receipt file, so parser drift hits the UI immediately. Verification: I compared the parser output against the raw session and read back the saved report.