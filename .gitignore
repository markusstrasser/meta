# Generated by bootstrap script

# ---- OS ----
.DS_Store
Thumbs.db

# ---- Editors ----
*.sw[po]
*~
.vscode/
.idea/

# ---- Lang/Framework Specific ----
node_modules/
bun.lockb
*.log
dist/
build/
.svelte-kit/
.cpcache/
.shadow-cljs/
.nrepl-port
target/
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.hypothesis/
.ipynb_checkpoints/

# ---- Secrets ----
.env
.env.*
.mcp.json
# The '!' is escaped to prevent issues if script is sourced in zsh
\!.env.example
**/.claude/checkpoint.md
**/.claude/checkpoint-autogen.md
.claude/scheduled_tasks.lock
**/.claude/current-session-id
**/.claude/checkout-claim.json
.claude/plans/
downloads/
.context/*.xml
.claude/overviews/
.claude/overview.conf
.claude/overview-marker*
.claude/overview-trigger.log

# Local run/log DB leftovers (superseded 2026-04-20 by ~/.claude/agentlogs.db)
runlog.db
runlogs.db
sessions.db

.project-upgrade/
.model-review/
bundles/*
bundles
artifacts/
.autoresearch-worktrees/
.claude/worktrees/
experiments/*/runs/
logs/
.brainstorm/
artifacts/prompt-archaeology/
.gstack/
docs/vendor/

# Session scratch dirs (capture real configs — never commit)
.scratch/

# Antigravity (agy) CLI per-session cache — stray, regenerable
.antigravitycli/

# corpus ingest checkpoints/indexes (regenerable state)
scripts/lesswrong_index.jsonl
scripts/lesswrong_ingested.txt
scripts/gwern_ingested.txt

.codex/

.agents/

# Derived/generated artifacts — track the GENERATOR + SOURCE, never the OUTPUT (#g 2026-06-18).
# A tracked generated file churns the history and drifts from its generator.
# Auto-generated by `just refresh-maps` (daily cron) — regenerated, not tracked
.claude/rules/codebase-map.md
.claude/maps/
artifacts/fable_throttle_probe.jsonl
.claude/skill-usage-watch.json
# Rendered diagram — derived from architecture.mmd (the tracked single source); regen via mmdc (see ARCHITECTURE.md)
architecture.png

# Drift sentinel digest (regenerated daily; auto-clears when green)
.claude/drift-digest.md
PRIORITIES.md
# Blindspot miner digest (regenerated daily; auto-clears when no flags)
.claude/blindspot-digest.md
# Maintain-tick runtime telemetry (append log; churns every tick — track the generator, not the log)
maintenance-actions.jsonl
# Orchestrator/debug pipeline run output in docs/audit/ — derived/ephemeral per run
# (debug-scout files, findings-consolidation handoffs, dispatch manifests).
# Durable codex-*/dispatch-research-* audits stay tracked.
docs/audit/*-debug-*
docs/audit/*-handoff.md
docs/audit/*-manifest.txt

# Downloaded paper PDFs (binaries — archival, not committed)
research/papers/

# Derived: daily deterministic output of scripts/sensor_integration_ranking.py — track the generator, not the output (#g 2026-06-18)
research/*-sensor-integration-ranking.md
