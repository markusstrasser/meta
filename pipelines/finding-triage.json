{
  "name": "finding-triage",
  "description": "Ingest session-retro findings into staging DB, auto-promote recurring findings, decay stale ones",
  "project": "meta",
  "steps": [
    {
      "step": "ingest-and-triage",
      "type": "script",
      "command": "uv run python3 scripts/finding-triage.py ingest artifacts/session-retro/ && uv run python3 scripts/finding-triage.py promote && uv run python3 scripts/finding-triage.py decay --days 30",
      "cwd": "~/Projects/meta"
    }
  ]
}
