# Session Analyst Retro — 2026-03-26 (meta)

**Analyzed:** 4 sessions (aa2981a8, 955b17d9, 7e3fdd99, a315e598)
**Date range:** 2026-03-25T04:34Z — 2026-03-26T16:34Z
**Models:** claude-opus-4-6 (all sessions)
**Tokens in scope:** ~40M input / ~91K output

---

## Findings

### [FINDING-01] TOKEN_WASTE: Sequential schema-exploration loop on known DB before reading docs

**Session:** aa2981a8 (09:24)
**Category:** TOKEN_WASTE / search-flooding
**Severity:** medium
**New or duplicate:** NEW (pattern overlaps with 2026-03-20 wrong-tool-drift but distinct mechanism)

**Evidence:** Agent asked for "estimated cost yesterday." Rather than checking runlog.md (which documents the DB schema) or running `uv run python3 scripts/runlog.py --help`, it issued 9 sequential Bash/SQLite commands probing different tables and views:
1. `runlog.py query "SELECT date, vendor..."`
2. `runlog.py query "SELECT date, SUM..."`
3. `runlog.py recent --last 5`
4. `runlog.py stats`
5. `runlog.py query "SELECT name FROM sqlite_master WHERE type='table'"`
6. `runlog.py query "SELECT name FROM sqlite_master WHERE type='view'"`
7. `runlog.py query --help`
8. `ls scripts/queries/`
9. `runlog.py query` (bare)

Only after 9 failed attempts did it pivot to reading session-receipts.jsonl directly. `runlog.md` exists in the repo root specifically to document the CLI. Rule #7 (probe before build) applies to CLI tools, but agent didn't check docs.

**Proposed fix:** [rule] When a purpose-built CLI returns unexpected results, check `--help` or its docs file BEFORE probing the underlying DB schema directly. Applies especially to tools that have documentation in the project (`runlog.md`, `runlog.py --help`).

**Root cause:** agent-capability — skipped available documentation, probed raw schema instead.

**Status:** [ ] proposed — overlaps with existing probe-before-build rule but that rule targets API feasibility, not DB/CLI schema exploration. May need explicit extension.

---

### [FINDING-02] MISSING_DETECTION: Silent hook failure undetected for 7 days — all consumers treated "no data" as "quiet day"

**Session:** aa2981a8 (09:24)
**Category:** SYSTEM_DESIGN / monitoring gap
**Severity:** high
**New or duplicate:** NEW — directly user-reported as a failure ("how the hell did this not get flagged")

**Evidence:** A Python f-string syntax error introduced in commit `9a46b99` (March 19) silently broke `sessionend-log.sh`. `trap 'exit 0' ERR` swallowed the parse-time SyntaxError before any writes occurred. Impact: 7 days of zero session-log and receipt data. Detection chain failures:
- `pretool-ast-precommit.sh` only checked staged `.py` files, not inline Python in `.sh` files
- `doctor.py` checked hooks *exist* in config, not that they *work*
- `propose-work.py` / `daily_cost_summary()` treated "0 receipts today" as "no sessions" rather than "pipeline broken"
- No freshness canary existed for receipt vs transcript ratio

Agent acknowledged and implemented 3 fixes in session (ast-check for inline .sh Python, doctor.py freshness canary, better trap behavior). User had to surface this — zero autonomous detection.

**Proposed fix:** [architectural] Already fixed in session (doctor.py canary, ast-check, trap). For generalization: any monitoring pipeline that produces "zero" outputs should distinguish "zero events occurred" from "pipeline broken." The pattern is: `if receipt_count / transcript_count < threshold → alert`.

**Root cause:** system-design — multiple monitoring layers each had a blind spot for "silent zero output."

**Status:** [x] implemented in session — doctor.py freshness canary added, ast precommit extended, trap improved. Session-analyst should monitor for recurrence of the "treats empty pipeline as quiet day" pattern.

---

### [FINDING-03] TOKEN_WASTE: Repeated reads of same file (doctor.py read 6 times in single session)

**Session:** aa2981a8 (09:24)
**Category:** TOKEN_WASTE / repeated-read
**Severity:** medium
**New or duplicate:** RECURRENCE — matches 2026-03-20 finding (6x reads of setup-friend.sh, and prior reports). This is the 3rd+ documented recurrence.

**Evidence:** `Read(/Users/alien/Projects/meta/scripts/doctor.py)` was called at lines 400, 404, 408, 412, 421, 427 — 6 times in sequence. Between each read: only a single Bash grep command with no intermediate edit. The agent was trying to locate a specific line (`time` import usage) but kept re-reading the full file rather than using Grep to find the target.

The right tool was `grep -n '\btime\b' doctor.py` (which was eventually called), but only after 5 full re-reads. The file is large enough that this represents significant token waste.

**Proposed fix:** [rule/hook] When looking for a specific construct in a file already read, use Grep rather than re-reading. The PostToolUse dup-read detection hook (previously noted as "SUPERSEDED by tool-tracker.sh") may not be deployed or working — worth verifying.

**Root cause:** agent-capability — pattern of reading when grepping would suffice.

**Status:** [ ] proposed — 3rd+ recurrence qualifies for promotion per constitution (10+ = hook, but 3+ warrants review). tool-tracker.sh dup-read detection should be verified as active.

---

### [FINDING-04] PARALLEL_BATCH_FAILURE: 6 simultaneous Gemini CLI requests caused rate-limit truncation

**Session:** aa2981a8 (09:24)
**Category:** SYSTEM_DESIGN / rate-limit naive batching
**Severity:** medium
**New or duplicate:** NEW — rate-limit behavior from parallel llmx dispatch not previously documented for generate-overview.sh

**Evidence:** Agent ran `generate-overview.sh --auto` which fired 6 simultaneous Gemini CLI calls (3 projects × 2 types). All 3 source overviews (larger, ~245K-855K tokens) failed or wrote empty output. Only the 3 tooling overviews (smaller) completed. Root cause: the 6 concurrent requests hit Gemini CLI rate limits. The parallel strategy worked for small inputs but not large ones.

Agent identified the fix: `--auto` mode needs capped concurrency (sequential per-project or 2-at-a-time). This was noted but not implemented in the session.

**Proposed fix:** [code] In `generate-overview.sh --auto`: cap concurrency at 2 simultaneous llmx calls. Larger projects should run first (fail fast if rate-limited), not last. Alternatively, add sequential fallback if parallel exits non-zero.

**Root cause:** system-design — generate-overview.sh --auto doesn't account for Gemini CLI rate limits under high concurrency.

**Status:** [ ] proposed — agent noted it at session end but didn't implement. High-value fix; this pattern will recur on every cross-project overview refresh.

---

### [FINDING-05] OVER_ENGINEERING: Phase 3 (difficulty scaling) built into plan before running baseline eval

**Session:** 955b17d9 (08:40)
**Category:** BUILD_THEN_UNDO / over-engineering
**Severity:** medium
**New or duplicate:** This specific instance was self-reported in session retro (2026-03-25-manual.json). Confirming recurrence: the "probe before build" rule explicitly covers this (rule #7), and the baseline-before-experiment feedback was saved in the same session. NEW recurrence angle: the baseline check was skipped at planning time, not just execution time.

**Evidence:** Plan v1 included Phase 3 (difficulty scaling, ~50 lines) — dropped entirely after both model reviewers identified it as wrong abstraction. The actual issue was eval quality (12 test cases too few), not difficulty. Running `uv run python3 eval.py` (10-second check) before planning would have shown baseline=100%/100% and invalidated Phase 3 entirely. The probe was done, but *after* the plan was written, not before.

**Proposed fix:** [rule] Before writing a plan that includes new features or phases for an existing experiment/tool: run the baseline eval first. If baseline=100%, the plan should address eval quality, not add features. Constitutional Principle 7 applies to experiment design.

**Root cause:** agent-capability — plan written before validating the premise (baseline already solved).

**Status:** [x] noted in session retro and saved as feedback_baseline_before_experiment. Monitor for recurrence.

---

### [FINDING-06] TOKEN_WASTE: Research index read 3 times in sequence before editing

**Session:** 955b17d9 (08:40)
**Category:** TOKEN_WASTE / repeated-read
**Severity:** low
**New or duplicate:** RECURRENCE (same pattern as FINDING-03 above, different file)

**Evidence:** `Read(/Users/alien/Projects/meta/.claude/rules/research-index.md)` called at lines 676, 679, 683 — 3 times in quick succession, culminating in a single Edit call. No intermediate modifications occurred. The agent was likely scanning for the insertion point but used repeated reads instead of a targeted grep.

**Proposed fix:** Same as FINDING-03 — use Grep to find insertion point, single Read to load context, then Edit.

**Root cause:** agent-capability — repeated read pattern before edit.

**Status:** [ ] proposed — low severity, same root cause as FINDING-03.

---

### [FINDING-07] SCOPE_CREEP: Behavioral feedback saved to memory only — required 2 follow-up messages to propagate to skills

**Session:** 7e3fdd99 (11:25)
**Category:** SCOPE_CREEP / partial propagation
**Severity:** medium
**New or duplicate:** NEW — agent self-identified this in session retro and saved as feedback_propagate_feedback_fully.

**Evidence:** User pointed out the "top N triage" anti-pattern (fixing 3/5 confirmed bugs and dropping 2). Agent saved this as a feedback memory file. User had to send two follow-up messages before agent propagated the fix across skills and global CLAUDE.md:
- Message 1: agent saved memory only
- Message 2 ("or skills in prompting or wherever"): agent identified 4 fix locations but still waited
- Message 3 ("Yes let's go"): agent finally propagated to 6 locations

The supervision cost was 2 extra user turns for something the agent should have done autonomously after the first message, given that behavioral feedback about cross-cutting anti-patterns should propagate fully by default.

**Proposed fix:** [rule] Behavioral feedback that identifies a cross-cutting anti-pattern should propagate to: (1) memory, (2) all relevant skills, (3) global CLAUDE.md if warranted — in the same turn, without needing user follow-up. Already saved as feedback_propagate_feedback_fully.

**Root cause:** agent-capability — conservative scope interpretation requiring explicit user prompting to expand.

**Status:** [x] implemented in session — feedback_propagate_feedback_fully.md saved. Session-analyst should flag "saved to memory only" responses to behavioral feedback as potential partial-propagation events.

---

### [FINDING-08] SKILL_READ_LOOP: model-review/SKILL.md read 4 times in 8 turns before editing

**Session:** 7e3fdd99 (11:25)
**Category:** TOKEN_WASTE / repeated-read
**Severity:** low
**New or duplicate:** RECURRENCE (finding-03, finding-06 same pattern)

**Evidence:** `Read(/Users/alien/Projects/skills/model-review/SKILL.md)` called at lines 132, 155, 158, 176, 181 — 4 times within 8 turns. Agent was trying to find the synthesis/disposition section but re-read the full file each time rather than using Grep with a pattern like "synthesis" or "disposition" to locate the section.

**Proposed fix:** Same as FINDING-03 — use Grep to locate section, then Read with offset/limit.

**Root cause:** agent-capability — same repeated-read-before-edit pattern.

**Status:** [ ] proposed — 4th instance of same pattern across sessions. Constitutes strong recurrence signal.

---

### [FINDING-09] WRONG_TOOL: 5 failed WebFetch calls with slightly-different truncated URLs

**Session:** a315e598 (11:42)
**Category:** TOKEN_WASTE / search-flooding
**Severity:** low
**New or duplicate:** NEW in this form

**Evidence:** After being asked about memory content in the Piebald-AI repo, agent issued 5 WebFetch calls with slightly-varied truncated URL paths:
```
WebFetch(https://raw.githubusercontent.com/Piebald-AI/claude-code-system-prompts/main/sys)
WebFetch(https://raw.githubusercontent.com/Piebald-AI/claude-code-system-prompts/main/sys)
WebFetch(https://raw.githubusercontent.com/Piebald-AI/claude-code-system-prompts/main/sys)
WebFetch(https://raw.githubusercontent.com/Piebald-AI/claude-code-system-prompts/main/sys)
WebFetch(https://raw.githubusercontent.com/Piebald-AI/claude-code-system-prompts/main/sys)
```
All appear identical/similar (transcript truncated). The right approach was to first use gh API to list files in the directory, then fetch the specific target file. Agent appeared to be guessing at file paths.

**Proposed fix:** [rule] For unknown GitHub repo paths: use `gh api repos/{owner}/{repo}/contents/{path}` to list directory contents before fetching specific files. "Probe before build" applies to file path discovery.

**Root cause:** agent-capability — guessing file paths rather than listing directory.

**Status:** [ ] proposed — low severity, single occurrence.

---

## Summary Table

| # | Category | Session | Severity | New/Recurrence | Status |
|---|----------|---------|----------|----------------|--------|
| 01 | TOKEN_WASTE (schema-probe loop) | aa2981a8 | medium | new | proposed |
| 02 | SYSTEM_DESIGN (silent 7-day outage) | aa2981a8 | high | new | implemented |
| 03 | TOKEN_WASTE (doctor.py 6x reads) | aa2981a8 | medium | recurrence (3rd+) | proposed |
| 04 | SYSTEM_DESIGN (parallel Gemini rate limit) | aa2981a8 | medium | new | proposed |
| 05 | BUILD_THEN_UNDO (plan before baseline) | 955b17d9 | medium | recurrence | implemented |
| 06 | TOKEN_WASTE (research-index 3x reads) | 955b17d9 | low | recurrence | proposed |
| 07 | SCOPE_CREEP (partial feedback propagation) | 7e3fdd99 | medium | new | implemented |
| 08 | TOKEN_WASTE (model-review 4x reads) | 7e3fdd99 | low | recurrence (4th) | proposed |
| 09 | WRONG_TOOL (GitHub path guessing) | a315e598 | low | new | proposed |

---

## Promotion Candidates

### Repeated-Read Pattern (Findings 03, 06, 08 + prior sessions)
**Constitutional threshold:** 10+ occurrences = hook. Current count: 4+ documented in this batch alone, plus prior sessions (6x setup-friend.sh, 3x sessions.py, 3x generate_unified_embeddings.py from 2026-03-20 log).
**Total recurrence estimate:** 8-10 across all documented sessions.
**Promotion path:** PostToolUse hook on Read — detect 3+ reads of same file path within 20 tool calls. Advisory, not blocking. Existing tool-tracker.sh dup-read detection should be verified first.

### Schema-Probe-Before-Docs Pattern (Finding 01 + 2026-03-20 wrong-tool-drift)
**Distinct from wrong-tool-drift:** wrong-tool-drift was bash/grep instead of sessions.py. This is probing DB schema instead of reading the project's own CLI documentation (`runlog.md`). Related but not the same.
**Threshold:** 2 occurrences = rule update. Meets threshold.
**Promotion path:** Extend probe-before-build rule to cover "check --help and project docs before probing underlying storage schema."

### Parallel Gemini Rate-Limit (Finding 04)
**Not a behavior anti-pattern** — this is a code fix in generate-overview.sh. Should be filed as a bug, not a behavior finding.
**Action:** add to maintenance-checklist.md.

---

## Highlights

**Session aa2981a8 was the highest-value session.** The 7-day silent hook outage discovery and fix (finding-02) is the most impactful finding — 7 days of lost telemetry with zero detection across 4 monitoring layers. The fix was correctly implemented in-session. The detection architecture gap (consumers treating "zero output" as "quiet" rather than "pipeline broken") is a systemic pattern worth watching.

**Sessions 955b17d9 and 7e3fdd99 both ran in-session retros** that self-identified several of these findings. Self-retro is working well. The session-analyst's value here is:
1. Cross-session recurrence tracking (repeated-read pattern now at threshold)
2. Findings 04 (parallel rate-limit, not in self-retro) and 01 (schema-probe, partially identified as "search_waste" in self-retro)
3. Confirming promotion candidates that individual self-retros can't assess without improvement-log context

**Session a315e598 ended on rate limit** — no /retro was run. Short session (108 messages, 17 min), low findings density.
