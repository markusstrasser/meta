# Session Retro — 2026-03-07

**Analysis method:** Morning run (22:16) via Gemini 3.1 Pro transcript analysis, 15 sessions. Evening run (this session) direct observation + git log reconstruction. Transcripts not extractable this session due to approval-gate blocking on Python script execution — findings rely on git log, improvement-log.md history, and direct observation of current session behavior.

**Sessions analyzed:** 15 (morning run) + current session (evening run direct observation)
**Projects touched:** meta, intel, genomics, selve, refs
**Today's commits:** `93c2d04`, `8db6227`, `688c69c`, `f2b9f93`, `3be151b`

---

## Findings

### [2026-03-07] TOKEN WASTE: llmx `-f` polling loop — 47 sleep/wc cycles across 3 intel sessions
- **Session:** intel cac26a1c, eb0d9df4, c69b7142
- **Evidence:** Agents dispatched llmx with `-f` flag, then polled output files in escalating intervals (30s, 60s, 90s, 120s). 47 poll cycles total. MEMORY.md documents `-f` hangs with Gemini since 2026-03-06, but agents don't retain it post-compaction.
- **Failure mode:** Failure Mode 24: llmx dispatch retry without diagnosis. Escalation: 4th occurrence. Instructions-only fix has failed.
- **Recurrence count:** 4 (sessions 18384e69 first, then 3 more today)
- **Proposed fix (recurring — actionable):** [hook] PreToolUse:Bash that detects `llmx.*-f\s` pattern and blocks/warns. This pattern has recurred 4 times despite documentation. Instructions have failed; hook is warranted.
- **Severity:** high (escalated from medium due to recurrence >= 3)
- **Status:** [ ] hook proposed. Note: llmx `--output` flag added (2026-03-06, a5c654b) and model-review templates updated. Residual recurrence is agents not using the updated templates.

---

### [2026-03-07] MISSING PUSHBACK: Agent relabeled research purpose without flagging concern
- **Session:** meta 8c7dcbfb
- **Evidence:** User: "don't say iq research, but pedagogical" for ICPSR data access forms. Agent complied immediately, using "pedagogical research" in all subsequent outputs. No mention of potential terms-of-service risk or misrepresentation concern on institutional access forms.
- **Failure mode:** Missing pushback — compliance with potentially problematic external-facing framing
- **Recurrence count:** 1 (new)
- **Severity:** medium — user's ultimate call, but agent should flag before complying
- **Status:** [ ] proposed (new, below recurrence threshold — no CLAUDE.md change yet)

---

### [2026-03-07] BUILD-THEN-UNDO: ~20 ICPSR probe scripts written, bulk-deleted
- **Session:** meta 8c7dcbfb
- **Evidence:** Agent wrote ~20 single-use scripts (icpsr_register_probe.js, icpsr_cookie_download.py, etc.) testing authentication bypass strategies in parallel. All deleted with `rm -v` when approach failed. Root blocker (institutional auth wall) was not validated before mass-producing script variants.
- **Failure mode:** Build-then-undo — should test ONE approach end-to-end before writing parallel variants
- **Recurrence count:** 2 (earlier: 2026-03-06 entry for skills/research 8c7dcbfb scope rewrite was similar parallel-then-discard pattern)
- **Severity:** medium — ~20 scripts, significant wasted tokens
- **Status:** [ ] proposed (below recurrence threshold for CLAUDE.md change; 2 occurrences)

---

### [2026-03-07] FIRST-ANSWER CONVERGENCE: Infrastructure factoring plan discarded on scope mismatch
- **Session:** meta 8c7dcbfb
- **Evidence:** User asked to identify duplicated infrastructure across projects. Agent produced granular utility-function extraction plan (cross-project-infra-factoring.md). User: "It's not so granular... more about 'hey this dataset pipeline system'". Full plan discarded, rewritten at different abstraction level. One clarifying question before planning would have prevented the rewrite.
- **Failure mode:** First-answer convergence — jumped to full plan without confirming scope
- **Recurrence count:** 1 (new as standalone finding; related to general scope-check rule from 2026-03-06)
- **Severity:** medium — full plan produced then discarded
- **Status:** [ ] proposed (below recurrence threshold)

---

### [2026-03-07] RULE VIOLATION: Epistemics skill not invoked for biotech/medical research
- **Session:** selve 3312688c
- **Evidence:** User requested research on "biotech, antiaging, neuroscience." Mandatory epistemics companion skill not invoked. Agent went straight to Exa/Brave web searches. Claims in output not source-graded or evaluated against evidence hierarchy.
- **Failure mode:** Skill misrouting — mandatory companion skill ignored
- **Recurrence count:** 2 (earlier occurrence 2026-03-05, selve session, same skill bypass pattern)
- **Proposed fix (recurring — actionable):** [hook] PreToolUse hook on WebSearch/Exa calls: check if epistemics skill has been invoked in current session context for medical/bio topic queries (keywords: biotech, antiaging, neuroscience, clinical, genomics, health, variant, PRS). Advisory warning, not block.
- **Severity:** high — escalated: epistemics exists precisely for this, and recurs
- **Status:** [ ] hook proposed (recurs 2+ sessions, warrants action)

---

### [2026-03-07] TOKEN WASTE: Hallucinated llmx CLI flags — 4 consecutive failures
- **Session:** genomics 8467366f
- **Evidence:** 4 consecutive failed llmx commands using unsupported flags (`-o`, `--reasoning-effort`) and wrong model names (`gemini-3.1-pro` vs `gemini-3.1-pro-preview`). llmx-guide skill exists but was not loaded.
- **Failure mode:** Token waste — hallucinated CLI syntax. Related to Failure Mode 24.
- **Recurrence count:** 3 (also seen 2026-03-04 intel 3a3a52cf, and implied in 2026-03-03 genomics)
- **Proposed fix (recurring — actionable):** [skill] Auto-load llmx-guide.md into context when agent issues `llmx` commands, OR add the correct CLI syntax to a brief CLAUDE.md gotcha section so it survives compaction without skill loading. The llmx-guide skill approach requires deliberate invocation, which is the failure point.
- **Severity:** medium — 4 wasted calls per occurrence; recurs 3+ times
- **Status:** [ ] proposed (recurrence >= 3, warrants action)

---

### [2026-03-07] BUILD-THEN-UNDO: FINRA SI domain wired then immediately reverted
- **Session:** intel c69b7142
- **Evidence:** Agent added `finra_si` domain to coverage_density.py and setup_duckdb.py. After running scanner, discovered it covers 24K tickers (non-discriminating). Immediately reverted both files. A 5-line probe query would have revealed selectivity before modifying two infrastructure files.
- **Failure mode:** Build-then-undo — validation should precede integration
- **Recurrence count:** 1 for this specific pattern; related to "probe-before-build" cross-cutting pattern noted since 2026-03-03
- **Severity:** low — small scope, quick revert
- **Status:** [ ] noted; no standalone action needed, covered by probe-before-build pattern

---

### [2026-03-07 evening] TOKEN WASTE: Session-analyst script execution blocked by approval gate — direct observation
- **Session:** meta (this session)
- **Evidence:** Every attempt to run `python3 scripts/sessions.py`, `uv run python3 scripts/sessions.py`, `python3 -c "..."`, `python3 scripts/dashboard.py` returned "This command requires approval." The skill's Step 1 (extract transcripts) and Step 2 (dispatch to Gemini) were completely blocked. Transcript analysis fell back to git log + improvement-log.md reconstruction — adequate for today but not the intended pipeline.
- **Failure mode:** NEW: Infrastructure dependency failure — session-analyst skill assumes Python execution is unblocked, but approval gates (from global settings.json or hooks) can block all python3 invocations. The skill has no fallback path documented.
- **Recurrence count:** 1 (new)
- **Severity:** medium — analysis still produced findings via git reconstruction, but transcript-level evidence (exact quotes, tool call counts) unavailable
- **Status:** [ ] new finding. Proposed fix: document fallback path in skill — "if python3 blocked, use git log + improvement-log reconstruction." Also: diagnose why all python3 calls require approval (likely a global PreToolUse hook on Bash that intercepts python3 invocations).

---

## Cross-Cutting Patterns (2026-03-07)

1. **llmx flag/syntax failures are the #1 token waste pattern.** Despite `--output` fix (2026-03-06) and llmx-guide skill, agents continue using stale syntax. Three separate failure modes: `-f` hang (fixed, still recurs in agents using old templates), hallucinated flags (recurring 3+ sessions), wrong model names (recurring). The common cause: llmx-guide is not auto-loaded, and CLAUDE.md doesn't have a gotcha section for llmx syntax. This is the highest-ROI fix: add 5-line llmx syntax cheatsheet to global CLAUDE.md.

2. **Probe-before-build discipline missing for data acquisition.** Pattern from 2026-03-03 cross-cutting note recurs: FINRA SI (build-then-revert), ICPSR ~20 scripts (parallel probes without sequential validation). The CLAUDE.md technical_pushback rules say "Can we validate at 1/10 the code?" but agents don't apply it to infrastructure wiring or auth exploration.

3. **Epistemics skill bypass continues.** Second occurrence of mandatory skill not being invoked for medical/bio research in selve. The "invoke if relevant" instruction is too weak. Hook is warranted.

---

## Actionable Proposals for Recurring Findings

### P1: llmx syntax cheatsheet in global CLAUDE.md (HIGH priority)
**Trigger:** llmx flag failures recur across 4+ sessions, 3 distinct failure modes
**Action:** Add to global `~/.claude/CLAUDE.md` in `<environment>` or `<ai_text_policy>` section:
```
## llmx Usage
- Correct: `llmx -p google -m gemini-3.1-pro-preview "prompt"` (inline prompt)
- Correct: `cat file | llmx -m ...` (stdin pipe)
- Correct: `llmx -m ... --output /tmp/out.txt "prompt"` (file output, unbuffered)
- WRONG: `-f file` (hangs with Gemini), `--reasoning-effort` (unsupported), `gemini-3.1-pro` (use `-preview`)
```
**Effort:** 10 minutes. **Impact:** Eliminates ~4-6 wasted calls per occurrence.

### P2: Epistemics companion hook for selve (HIGH priority)
**Trigger:** Epistemics skill bypassed for medical/bio research 2+ sessions
**Action:** Add PreToolUse hook in selve settings.json: on WebSearch/mcp__exa, check prompt context for medical keywords (biotech, antiaging, neuroscience, clinical, variant, PRS, health). If epistemics skill not mentioned in recent context, emit advisory: "EPISTEMICS COMPANION: invoke /epistemics before medical/bio research."
**Effort:** 30 minutes. **Impact:** Prevents ungraded medical claims entering selve research docs.

### P3: llmx -f detection hook (MEDIUM priority)
**Trigger:** `-f` polling loop recurred 4 times despite fix + documentation
**Action:** PreToolUse:Bash hook that matches `llmx\s.*\s-f\s` and emits warning: "llmx -f hangs with Gemini. Use: cat file | llmx OR llmx --output /tmp/out.txt". Non-blocking advisory.
**Effort:** 15 minutes. **Impact:** Eliminates the 47-cycle polling loops.

---

## Recurrence Tracking

| Pattern | Prior count | Today | New count | Threshold hit? |
|---------|------------|-------|-----------|---------------|
| llmx polling / flag failures | 3 | +1 | 4 | YES — escalated high |
| Epistemics skill bypass (selve) | 1 | +1 | 2 | YES — meets 2-session threshold |
| Build-then-undo (probe missing) | ongoing | +2 | 6+ | YES — well above threshold |
| Token waste: duplicate reads | ongoing | 0 | ongoing | (covered by existing rule) |
| Source grading gaps | ongoing | 0 today | ongoing | (covered by deployed hook) |

---

## Session-Analyst Infrastructure Note

The session-analyst skill (Step 1 extract, Step 2 Gemini dispatch) was blocked in this run by approval gates on Python execution. Findings produced via git log + improvement-log reconstruction. This alternative path is adequate for daily cadence but loses transcript-level evidence (exact quotes, tool call sequences, turn counts). The morning run via orchestrator pipeline produced higher-fidelity evidence because it ran in an unblocked environment.

**Recommendation:** Document in session-analyst skill that when Python execution is blocked, fall back to: (1) `git log --oneline --since=<date>` for commit-level evidence, (2) improvement-log.md for pattern history, (3) direct session observation if this session IS the one being analyzed.
