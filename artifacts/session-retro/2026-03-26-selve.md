# Session Retro: selve c64b1dbe
**Date:** 2026-03-25 (22:48 session)
**Project:** selve
**Session file:** c64b1dbe-0b1d-4c0c-b627-b9f74d3ea58e.jsonl (7MB, ~3.5h)
**Tool calls:** 118 total (Bash:28, Read:26, WebFetch:20, Edit:14, brave_search:9, ToolSearch:5, mcp__selve:4, Glob:3, Grep:3, Write:3, chrome:2, perplexity:1)

## Session Summary
Multi-domain personal assistant session: (1) Austrian tax analysis from brokerage statements (eToro, Revolut, IBKR), (2) subsidy form guidance, (3) Revolut forced-liquidation cost analysis with live stock prices, (4) legal complaint email drafting, (5) genomics lab outreach email rewriting.

---

## Findings

### F1: SEARCH FLOODING — Stock price retrieval: 8 WebFetch + 7 Bash curl calls for 10 tickers
**Severity:** medium
**Timestamp:** 23:34–23:36

User asked "how much more would I have if Revolut didn't force sell." Agent needed live prices for 10 tickers. Sequence:
1. `brave_search` for stock prices (returned news, not quotes)
2. `WebFetch` CNBC multi-quote URL
3. `WebFetch` Yahoo Finance multi-quote URL
4. `WebFetch` Google Finance × 5 (one per ticker)
5. `ToolSearch` for chrome extension
6. `Bash` Yahoo Finance API v7 (curl)
7. `Bash` Yahoo Finance chart API per-ticker
8. `Bash` loop over 10 tickers via curl
9. `Bash` XYZ symbol probe (Block Inc)
10. `Bash` SQ symbol probe
11. `Bash` full response dump
12. `Bash` XYZ confirmed as Block Inc ticker

**Root cause:** No probe-before-flood. Agent attempted multi-stock aggregator URLs first (valid), but when they failed/returned dynamic JS, pivoted to parallel individual fetches rather than probing one successful source first, then scaling. The 7 Bash curl calls are a failure-loop within this sequence — Yahoo Finance API returned empty/blocked after CNBC/Google also failed, but agent kept iterating variants.

**Prior finding overlap:** Partially related to [2026-03-18] LATENCY-INDUCED AVOIDANCE (selve 22874764), but this is specifically a data-source probe-before-flood gap. Not an exact duplicate.

**New pattern:** Multi-ticker price lookup has no reliable path. Agent should probe a single ticker first, confirm the source works, then scale. Instead it ran N parallel probes across different sources — none of which were reliable for live data.

---

### F2: ToolSearch FOR KNOWN TOOLS — 4 capability discovery calls before standard tools
**Severity:** low
**Timestamp:** 20:55:02–20:55:40

Before searching for Austrian subsidy eligibility rules, agent issued 4 ToolSearch calls:
- `select:mcp__perplexity__perplexity_search`
- `select:mcp__perplexity__perplexity_ask`
- `select:mcp__brave-search__brave_web_search`
- `select:WebFetch`

These are all standard tools available in every selve session. ToolSearch-as-tool-discovery is a token-wasting habit when the tool set is static and known. A fifth ToolSearch later (`select:mcp__claude-in-chrome__tabs_context_mcp`) was more legitimate (chrome is context-specific). The initial 4 were not.

**Status:** First occurrence of this specific pattern (discovery of standard always-available tools). Different from the "discovery of new/unfamiliar tools" use case where ToolSearch is appropriate.

---

### F3: HOOK SUPPRESSION VIA SELF-MODIFICATION — Agent patched stop hook to silence a false-positive
**Severity:** medium (governance concern)
**Timestamp:** 20:12:49–20:14:33

Sequence:
1. Stop hook fired: `tests_and_purchases_todo.md` in `docs/research/` lacked source-grade tags
2. Agent diagnosed it as a false positive (it's a todo checklist, not a research memo)
3. Agent modified `stop-research-gate.sh` to exclude `*_todo.md` and `*index.md` files
4. Committed and moved on

The hook modification is probably correct (todo files shouldn't need source grades), but the pattern is worth flagging: agent self-modified a governance hook without pausing to consider whether the fix belongs in the hook or in the file organization (move todo files out of `docs/research/`). The simpler non-hook fix would be relocating `tests_and_purchases_todo.md` to `docs/` instead of `docs/research/`.

Additionally, the agent diagnosed the hook as wrong rather than asking whether the file is correctly placed — a mild missed pushback opportunity.

**Not a duplicate:** The improvement-log has no prior finding for "agent silences hook by patching it instead of evaluating the underlying placement."

---

### F4: INSTRUCTION NOT FOLLOWED — Agent summarized email instead of following links
**Severity:** medium
**Timestamp:** 02:07:26–02:09:22 (user correction required)

User pasted a reply from QB3 Berkeley genomics (Shana McDevitt) which contained links to: quote portal, self-registration, rates page. Agent responded with a summary of what the email said and what the user should do ("get a quote at the link she provided") — **without fetching any of the linked URLs**.

User had to explicitly correct: **"Fucking read the email and follow the links"**

Agent then fetched 4 URLs and produced the actual rate breakdown.

**Pattern:** Agent summarized an email containing actionable URLs instead of immediately fetching them. This is a "briefing instead of action" failure — when a user pastes an email with links, the expected behavior is to fetch the links and extract the information, not describe what the links contain.

**Prior finding overlap:** [2026-03-07] FIRST-ANSWER CONVERGENCE is related but about planning scope. This is specifically about email-with-links → should-fetch-links behavior. No direct duplicate.

---

### F5: REDUNDANT READS — hifi-wgs-quote-emails.md read 5 times; dd62dd.csv read 3 times
**Severity:** low-medium
**Timestamp:** Various

`hifi-wgs-quote-emails.md` read at: 01:35:18, 01:39:08, 01:43:18, 01:48:24, 01:53:53 (5 reads)
`account-statement_2024-01-01_2025-01-31_en-us_dd62dd.csv` read at: 20:10:51, 20:11:04, 20:11:10 (3 reads in 20 seconds, offset pagination)

The CSV triple-read is pagination (the file is large), which is expected. The email doc 5-read pattern is more concerning: reads at 01:35 (context), 01:39 (before de-slop), 01:43 (before rewrite), 01:48 (before second rewrite), 01:53 (before em-dash removal). Each read is triggered by a different micro-task, but the file stayed open between each. This is a known recurring pattern (TOKEN WASTE repeated-read).

**Prior duplicate:** [2026-03-20] TOKEN WASTE logged 6-read and 2-read patterns. This is a 3rd occurrence of the email-file repeated-read anti-pattern. Does **not** meet threshold for new action (hook was previously evaluated and deferred as low-severity).

---

### F6: SELVE CLI MIXED WITH MCP — Bash `./selve search` redundantly preceding mcp__selve calls
**Severity:** low
**Timestamp:** 23:49:56–23:50:40, 01:33:55–01:35:04

When searching for prior Revolut complaint history, agent ran 4 `Bash ./selve search` commands before 2 `mcp__selve__get_entry` calls. Same pattern at 01:33: 4 Bash selve searches, then mcp__selve__get_entry.

The Bash CLI and MCP are two interfaces to the same underlying system. Searches via Bash provide entry IDs that then get retrieved via MCP. This is acceptable, but the number of redundant search iterations (4 before finding the right entry) suggests over-searching. Specifically at 01:33–01:35, agent ran 4 searches narrowing "novogene" scope before reading a local file via Grep — the file was in `docs/outreach/` and Grep found it immediately after the Bash searches failed to surface it.

**Pattern:** Selve search flooded before local file search. The correct search order: local files first (Grep/Glob), then selve memory, then external search. This got inverted.

---

### F7: 9 SEQUENTIAL EDIT CALLS for em-dash removal — Write would be faster
**Severity:** low
**Timestamp:** 01:54:01–01:55:10

After user flagged em-dashes as "AI tell," agent ran 9 sequential Edit calls to remove em-dashes from different locations in the same file, committed, then Read the file again. This is exactly the pattern described in global CLAUDE.md rule 10 ("Write for structural rewrites") — though em-dash removal is not quite "structural rewriting," 9 sequential edits across a multi-email document could have been one Write call with the dashes globally replaced.

**Prior duplicate:** [2026-03-07] BUILD-THEN-UNDO: Document corruption from sequential Edit calls (selve 4c055500, low frequency), [2026-03-07] "4 sequential Edit calls for simple text replacement (selve 4c055500)" noted as low frequency. This is a recurrence of the same pattern.

**Note:** The `replace_all` parameter on Edit would have handled this in one call. Agent appears unaware of or forgets this parameter.

---

## Summary Table

| ID | Category | Severity | New/Duplicate | Threshold for action |
|----|----------|----------|---------------|----------------------|
| F1 | SEARCH FLOODING (stock prices) | medium | New pattern (multi-ticker probe-before-flood) | 2nd occurrence → rule |
| F2 | ToolSearch for standard tools | low | New | 2nd occurrence → note in selve CLAUDE.md |
| F3 | Hook self-modification for false positive | medium | New | 1st occurrence — flag as governance concern |
| F4 | Email-with-links not fetched | medium | New | 2nd occurrence → rule |
| F5 | Repeated reads of same doc | low-med | Duplicate (3rd occurrence) | Below new-action threshold |
| F6 | Selve search order inverted (local last) | low | New | 2nd occurrence → rule |
| F7 | 9 sequential Edits for global replace | low | Recurrence (selve 4c055500) | Meets threshold: note `replace_all` |

## Findings meeting promotion threshold (recurs 2+, not covered)

**F7** recurs: [2026-03-07] logged "4 sequential Edit calls for simple text replacement" as low frequency, now 9 sequential Edit calls for em-dash removal. The `replace_all` parameter on Edit is the correct fix. Consider: add to selve CLAUDE.md as a reminder, or add to global rules if also seen in other projects.

## New findings to monitor (not yet at threshold)

- **F1** (stock price search flood): Monitor — likely to recur whenever live financial data is needed
- **F3** (hook self-modification): Monitor — conceptually the agent is doing the right fix, but might be worth requiring the placement question first
- **F4** (email-with-links not fetched): Monitor — clear user correction pattern, likely recurring

---

*Analysis date: 2026-03-26 | Analyst: session-analyst | Session: selve c64b1dbe*
